fun main(){

    val name = "Bhumika"       // val is read-only (cannot be changed)
    println(name)

    var age = 22                // var is mutable (can be changed)
    println(age)

    age = 24                    // Updating the value of mutable variable
    println(age)

    val customers = 10          //String templates
    println("There are $customers customes")
}